import { Card } from "primereact/card";

const Home: React.FC = () => {
  return (
    <Card title="Home">
      <p>Welcome Home!</p>
      <p>ユーザ名は右上から変更できます。</p>
    </Card>
  );
};

export default Home;
